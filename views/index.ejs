<%- include('header.ejs') %>

<div class="mb-3">
    <a href="/new" class="btn btn-primary">New</a>
</div>


<%
    for(let i = 0; i < values.length; i++) {
        let v = values[i];
%>
<div class="card mb-3">
    <div class="card-header">
        <h5 class="card-title"><%= v.dataValues.id %></h5>
    </div>
    <div class="card-body">
        <p class="card-text"><%= v.dataValues.message %></p>
        <a href="/edit/<%= v.dataValues.id %>" class="btn btn-primary">Edit</a> | 
        <a href="#" class="btn btn-danger">Delete</a>
    </div>
</div>
<%
    }
%>

<%- include('footer.ejs') %>